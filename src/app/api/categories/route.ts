// This file is intentionally left blank.
// The /api/categories route was removed because it's not compatible with
// the static export (`output: 'export'`) needed for deployment on this hosting plan.
// The category data is now loaded directly from `src/lib/data.ts` in the `use-categories.ts` hook.
